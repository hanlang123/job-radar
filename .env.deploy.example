# 部署服务器配置示例
# 复制此文件为 .env.deploy 并填入真实值（.env.deploy 已 gitignore）
DEPLOY_SERVER=YOUR_SERVER_IP
DEPLOY_USER=ubuntu
DEPLOY_PASSWORD=YOUR_PASSWORD
# AI API Key
OPENAI_API_KEY=your-api-key-here

# ============================================================
# GitHub Actions 自动部署所需的 Secrets 配置
# ============================================================
# 请在 GitHub 仓库 Settings → Secrets and variables → Actions 中添加以下 Secrets:
#
# ---- 服务器连接 ----
# DEPLOY_SERVER        - 部署目标服务器 IP 或域名
# DEPLOY_USER          - SSH 登录用户名（如 ubuntu / root）
# DEPLOY_SSH_KEY       - SSH 私钥（推荐使用 Ed25519 密钥对）
# DEPLOY_PASSWORD      - SSH 密码（如果不使用密钥认证）
#
# ---- 应用密钥 ----
# OPENAI_API_KEY       - OpenAI 兼容 API Key
# JWT_SECRET           - JWT 访问令牌密钥
# JWT_REFRESH_SECRET   - JWT 刷新令牌密钥
#
# ---- 镜像仓库（GHCR 默认使用 GITHUB_TOKEN，若服务器需拉取则需配置） ----
# GHCR_PAT             - GitHub Personal Access Token（需有 read:packages 权限）
#                        用于部署服务器从 GHCR 拉取镜像
#
# ---- 可选：GitHub Environment ----
# 建议创建名为 "production" 的 Environment，启用保护规则（如审批、分支限制）
